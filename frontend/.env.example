# 本地开发环境变量模板
# 用法：复制为 .env.local（或 .env.development.local）后按需修改，该文件不提交密钥。
#
# §14.1 生产环境零 Mock 数据开关：
#   - 仅当 import.meta.env.DEV 为 true（即 vite dev / build --mode development）且本开关为 'true' 时，
#     前端才注入 mock 假数据用于演示；
#   - PROD 构建（vite build 默认 production 模式）下该值被忽略，MOCK_ENABLED 恒为 false，绝不注入 mock。
#   - 使用 mock 的界面会渲染 DEMO 角标并降级为 STALE 徽章（见 DataSourceBadge）。
VITE_ENABLE_MOCK=true
