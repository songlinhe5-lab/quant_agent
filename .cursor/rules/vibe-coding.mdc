---
description: Cursor adapter. Coding SSOT is /AGENTS.md — do not duplicate it.
alwaysApply: true
---

# Cursor 适配器（非宪法）

编码宪法是仓库根 **`AGENTS.md`**。本文件只服务 Cursor：frontmatter + 下面 fail-safe。**禁止在此另立规则。** 与 `AGENTS.md` 冲突时以 `AGENTS.md` 为准。

- 已在上下文中则不要再 Read `AGENTS.md`。
- 不要 `@` 整本 `docs/02` / `AI_INSTRUCTIONS.md` / `docs/06`；按 `AGENTS.md` §8 按需加载。
- 盘中 Hermes 人设在 `prompts/system/HERMES.md`，编码任务忽略它。
- 核心区改动先 Plan 等人确认（`docs/02` §7.6）。

## Fail-safe（仅当 AGENTS.md 未注入时生效）

1. 禁止 Next.js / Nuxt / 任意 SSR；禁止 Vue / Redux / Axios。
2. K 线主图只用 Lightweight-Charts，禁止 ECharts。
3. Tick 禁止进 `useState`，用 `useRef` + DOM 突变。
4. 主服务禁止安装/import `futu-api` 等数据 SDK；禁止前端/Hermes Tools 直连外网数据源。
5. `routers/` 禁止业务逻辑与同步阻塞。
6. 实盘必须先检查 `REAL_TRADE_EXECUTE`（默认 false）。
7. Futu 百分比传小数（ROE 15% → `0.15`）。
8. Hermes 代码保持 Plan → Tool → Verify → Output；同一 Tool 连败 3 次熔断。
9. 禁止提交 `.env`；禁止 `git push --force` 到 `main`。
10. 生产镜像禁止 `:latest`。
11. 日志用 structlog，禁止 `print()`。
12. 单文件超过 300 行拆分；禁止超过 1000 行。
13. 单测禁止打真实 Redis / PG / Futu / 外网。
14. 新 API 先 Pydantic 再路由；响应 `{status, message, data, timestamp}`。
15. 开发在 `develop` 链；`main` 只接受来自 `develop` 的 PR。
