name: flutter_app
description: Quant Agent Flutter thin client (Android / iOS / HarmonyOS) — CLI-01 scaffold
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  dio: ^5.10.0
  flutter_secure_storage: ^10.3.1
  equatable: ^2.1.0
  web_socket_channel: ^3.0.3
  local_auth: ^2.3.0
  flutter_markdown: ^0.7.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
