Python 脚本 + Shell 脚本；后端通过 `uvicorn backend.main:app --reload` 启动 FastAPI 服务；压测使用 Locust（`locust_ws_stress.py` + `locust.conf`）；部署使用 systemd service 单元与 Tailscale；验证结果输出 JSON（如 `api_smoke_report.json`）。