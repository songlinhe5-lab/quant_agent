评估运行需先准备 `backend/app/eval/golden_dataset.json`，否则 `load_golden_dataset()` 会直接返回空列表；专家团会话默认写入 Redis key 前缀 `quant:expert_team:`，TTL 12 小时，PG 表名为 `ExpertTeamSession`；RAG 治理依赖 `webpage_knowledge_base` 表中的 `embedding_model_version` 字段进行版本比对。