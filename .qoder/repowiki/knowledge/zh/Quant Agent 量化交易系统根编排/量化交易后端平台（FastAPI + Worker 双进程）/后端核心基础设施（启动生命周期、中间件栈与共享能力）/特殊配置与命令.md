启动时通过环境变量 `.env` 注入 DATABASE_URL、REDIS_*、LLM_*、EMBEDDING_*、FUTU_*、FEISHU_WEBHOOK_URL 等配置；`QUANT_ENV=development` 会放宽限流阈值并启用更多调试行为；`FORCE_RESET_ADMIN=true` 可在启动时重置默认 admin 密码；`L1_CACHE_MAX_SIZE`、`REDIS_MAX_CONNECTIONS`、`DB_POOL_SIZE` 等可调参数影响运行时资源占用。
