JWT 密钥与 Google Client ID 通过环境变量 `SECRET_KEY`、`GOOGLE_CLIENT_ID` 注入；Refresh Token Cookie 的安全属性（SameSite/Secure）根据 `QUANT_ENV==production` 动态切换；FMP credit 指标默认从 `http://localhost:8001/metrics` 拉取，可通过 `FMP_REMOTE_URL` 覆盖。
