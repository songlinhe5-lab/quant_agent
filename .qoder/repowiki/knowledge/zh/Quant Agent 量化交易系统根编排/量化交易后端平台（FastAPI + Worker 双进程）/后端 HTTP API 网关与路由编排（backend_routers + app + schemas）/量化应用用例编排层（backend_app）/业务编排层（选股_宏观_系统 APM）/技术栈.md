FastAPI 异步函数 + Pydantic v2 BaseModel 定义请求体；Redis 缓存（setex TTL + ZSET 滑动窗口）；asyncio.Lock 细粒度锁池防缓存击穿；Pandas 内存引擎用于横截面表达式计算；SQLAlchemy ORM 直接操作 `models.SavedScreen` / `ScreenerSubscription` / `PerformanceLog`；通过 `backend.app.market_data` 适配器对接 Futu OpenD、AKShare、FRED、DBNomics 等多数据源。
