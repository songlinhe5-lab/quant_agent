运行需准备 `backend/data/kline_warehouse` 目录存放热数据，`backend/data/snapshots` 由 `ensure_roots()` 自动创建；通过环境变量 `DATALAKE_LIVE_ROOT`、`DATALAKE_SNAPSHOTS_ROOT` 覆盖路径，`ENGINE_ALLOW_LIVE_DATA=true` 才允许 `live` 模式，`DATALAKE_DIRTY_RATE_MAX` 控制质量门限；R2 冷归档需设置 `KLINE_L3_ENDPOINT_URL`、`KLINE_L3_BUCKET`、`KLINE_L3_ACCESS_KEY`、`KLINE_L3_SECRET_KEY`、`KLINE_L3_REGION`、`KLINE_L3_PREFIX`。
