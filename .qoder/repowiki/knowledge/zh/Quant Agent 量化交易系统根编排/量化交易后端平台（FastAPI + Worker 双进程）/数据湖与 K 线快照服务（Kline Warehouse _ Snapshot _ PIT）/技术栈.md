Parquet (PyArrow) 列式存储；PostgreSQL via SQLAlchemy ORM (`DataSnapshot` 模型)；Redis 分布式锁与 `latest_snapshot_id` 指针；Cloudflare R2 / S3 兼容对象存储（`pyarrow.fs.S3FileSystem`）；pandas + pyarrow 进行冷热数据读写；structlog 结构化日志。
