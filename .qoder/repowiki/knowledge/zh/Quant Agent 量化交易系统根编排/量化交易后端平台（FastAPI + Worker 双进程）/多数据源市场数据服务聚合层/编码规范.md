- 每个外部数据源在 adapters/ 下提供独立文件并实现 DataSourceInterface，由 __init__.py 统一导出后在应用启动时注册到 DataSourceRegistry
- 业务侧通过 business/ 下的门面类（market/fundamental/macro/option）发起请求，不直接依赖具体适配器实现
- 所有数据访问统一返回 Result[Data, ErrorInfo] 结构，错误信息包含 code/message/desc 以便熔断与告警处理
- 对下游调用统一经 throttler.py 限流与熔断器保护，异常路径走自适应退避而非直接抛错