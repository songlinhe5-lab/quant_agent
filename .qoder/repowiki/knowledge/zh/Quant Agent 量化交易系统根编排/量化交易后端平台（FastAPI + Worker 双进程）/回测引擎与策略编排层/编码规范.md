- 策略统一继承 backend.engine.Strategy 并通过 on_bar/on_tick 接收历史窗口 DataFrame，使同一策略可在 backtest 事件驱动、engine 驱动与 live 环境中复用。
- 所有运行产物通过 backend.engine.contracts.RunManifest 携带 code_hash、manifest_hash、random_seed、data_mode 等字段，供 report_service 计算可复现指纹。
- 批量回测（网格搜索、蒙特卡洛、横截面）均以 runners.py 中 run_*_backtest 函数为统一入口，内部委托 _drive_strategy 驱动策略执行。
- 动态策略源码一律经 sandbox._verify_safe_code 进行 AST 白名单校验后再 exec，禁止直接 eval 用户输入。
