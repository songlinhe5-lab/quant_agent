- 所有子模块通过 `backend.engine.Strategy` 基类与 `contracts.py` 中的数据契约交互，禁止直接耦合具体驱动实现
- 订单出口统一经由 `gateway.py` 的 OrderGateway 路由，按运行环境选择 backtest/live/sim_broker 后端
- 时钟访问一律通过 `Clock/SimClock/WallClock` 抽象，禁止直接使用系统时间或模拟时间对象
- 分析工具（网格搜索/蒙特卡洛/Walk-Forward）均以 VectorExecutor 为默认执行路径，保证与实盘策略逻辑一致