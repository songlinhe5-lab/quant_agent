asyncio 协程 + Redis pub/sub（`quant:quotes:stream` 等键）作为进程间通信总线；Pydantic 模型用于规则/事件序列化；pandas DataFrame 用于 CEP 指标计算；Prometheus client 暴露数据质量指标；Futu SDK 仅存在于 data_subservice，主服务通过 `data_source_router.fetch_futu` HTTP 调用。
