采集器默认全部开启，不再使用 `COLLECTOR_*` 环境变量门控；新增采集器需在 `collector_registry.COLLECTORS` 中注册 `CollectorDef(name, factory)` 并在 `collectors/<name>.py` 实现 `start()` 返回协程列表；Finnhub 相关守护进程需配置 `FINNHUB_API_KEY` 才能启动新闻监控。
