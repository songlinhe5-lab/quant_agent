pytest + pytest-asyncio + fastapi TestClient；SQLAlchemy（SQLite 内存库）；Redis Mock（AsyncMock 管道/pipeline/pubsub）；vcrpy YAML cassette（finnhub/fmp/fred/futu/yfinance 三方接口离线回放）；内置 ThreadingHTTPServer 作为契约子服务。
