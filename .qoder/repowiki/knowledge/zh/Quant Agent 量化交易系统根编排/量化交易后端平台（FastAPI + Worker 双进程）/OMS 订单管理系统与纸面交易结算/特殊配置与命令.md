策略文件需放置在 `backend/workers/strategies/live/<class_name>.py`，运行时由 `BotRuntimeManager` 动态 import；算法拆单与 Bot 生命周期依赖 Redis 键空间 `quant:oms:*`，需在启动前确保 Redis 可用；EOD 结算通过 `PaperSettlementDaemon.run()` 以 300 秒间隔循环，使用 Redis NX 锁 `quant:lock:paper_settle:{market}:{date}` 保证幂等。