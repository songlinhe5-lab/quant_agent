RAG 词库热更新需调用 `reload_indicators()`：优先从 `backend/data/indicators.csv` 加载指标规则，若存在 `EMBEDDING_API_KEY` 则走云端 Embedding 向量化并写入 `quant_screener_rules` 表（自动 ALTER 列维度至 `EMBEDDING_DIM`）；否则降级为本地 SentenceTransformer 或全量规则兜底。
