需配置环境变量 `FEISHU_WEBHOOK_URL`/`FEISHU_SECRET`、`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` 以启用对应通道；引擎通过 Redis 键空间 `quant:alerts:rules`、`quant:alerts:events`、`quant:alerts:prices`、`quant:alerts:cooldown:*`、`quant:alerts:dlq` 与外部交互，需确保 Redis 可用。
