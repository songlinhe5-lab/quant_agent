通过环境变量控制行为：`DATASOURCE_PORT`（默认 8001）、`DATA_SOURCE_HMAC_SECRET`（HMAC 密钥）、`DS_CAPABILITIES` 或 `NODE_CAPABILITIES`（逗号分隔的能力白名单，未声明则返回 503）、`YF_MAX_CONCURRENCY`（yfinance 并发信号量，默认 8）、`YF_THREAD_WARN`（线程告警阈值，默认 800）、`ENABLE_REDIS_HEARTBEAT`、`NODE_HEARTBEAT_TTL`、`NODE_ID`、`NODE_WEIGHT`、`FUTU_HOST/FUTU_PORT`。测试使用 pytest（`pytest.ini` 配置），覆盖率输出至 `.coverage`。
