FastAPI + uvicorn 作为 HTTP 框架；httpx 用于子节点间 HMAC 签名调用；prometheus_client 暴露 metrics；Redis 作为分布式注册中心（Hash/ZSet/Set）；yfinance、akshare、tushare、futu（OpenD）、finnhub、fmp、fred、dbnomics、rbi、tavily/bocha/jina 等第三方数据源 SDK 通过延迟 import 按需加载。
