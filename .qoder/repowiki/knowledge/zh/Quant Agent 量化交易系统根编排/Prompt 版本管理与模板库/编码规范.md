- 每个 Prompt 文件头部使用 YAML Front Matter 声明 id、name、target_model、input_variables、output_format、last_tested、eval_score、changelog 等元信息
- 新增 Prompt 时基于 `templates/_template.md` 的骨架文件创建，保持字段结构一致
- 系统级 Prompt 与 IDE 编码宪法严格分离：运行时指令放在 `prompts/system/HERMES.md`，IDE 规则放在仓库根 `AGENTS.md`，禁止混用
