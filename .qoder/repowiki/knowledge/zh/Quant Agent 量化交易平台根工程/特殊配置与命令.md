`make install` 使用 uv sync --all-extras 初始化环境并安装 pre-commit；`make dev` 一键拉起 Redis/PG、后端 API、Worker 与前端；`make test` 运行 backend/tests 单测；`make test-cluster` 执行集群通信端到端验证；`make coverage` 生成 htmlcov 覆盖率报告；`pnpm run proto:gen` 从 shared/proto 生成前端 protobuf 类型。