运行前需设置 `QUANT_ENV=testing`、`DATABASE_URL=sqlite:///./test.db`、`REDIS_HOST=localhost`、`BCRYPT_ROUNDS=4`、`ENCRYPTION_MASTER_KEY` 等环境变量；启用真实网络/外部 API Key 的用例需加 `@pytest.mark.live_network` 标记；录制新 cassette 需设置 `QUANT_RECORD=1` 触发 re-record。