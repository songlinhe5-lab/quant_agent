需配置环境变量：`FINNHUB_API_KEY`（Finnhub）、`FMP_API_KEY`（FMP）、`TUSHARE_TOKEN`（Tushare）、`PROXY_POOL`（Finnhub 代理池逗号分隔）、`AKSHARE_MODE=direct|cache`（默认 direct）、`DATASOURCE_TUSHARE_MODE=internal|external`（默认 internal）。Finnhub 免费档 60 req/min 极易触发 429，建议开启代理并配合 SVC-08 退避引擎。