# API 接口规范 (V1.0)

> **文档定位**：本文档是前端、客户端与后端开发的接口契约（Contract）。所有接口的 URL、方法、鉴权方式、请求/响应 Schema、错误码一律以本文档为准。  
> **最后更新**：2026-06-27 | **版本**：V1.0  
> **关联文档**：`docs/03. 后端架构与执行引擎.md` | `docs/11. 数据模型与领域设计.md`

---

## 一、全局约定

### 1.1 Base URL

| 环境 | Base URL |
|:---|:---|
| 本地开发 | `http://localhost:8000` |
| 香港 VPS（生产） | `https://api.quant.yourdomain.com`（经 Cloudflare Tunnel） |

### 1.2 通用响应结构

所有 REST API 统一返回以下 JSON 结构，**严禁**各路由自定义格式：

```json
{
  "code": 0,
  "msg": "ok",
  "data": {},
  "ts": 1719475200000
}
```

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `code` | `int` | `0` = 成功，非零 = 错误（见错误码表） |
| `msg` | `str` | 成功时为 `"ok"`，失败时为可读错误描述 |
| `data` | `any` | 业务数据负载，失败时可为 `null` |
| `ts` | `int` | 服务端 UTC 毫秒时间戳 |

### 1.3 鉴权方式

| 通道 | 鉴权机制 | Header |
|:---|:---|:---|
| 公开 REST API（`/api/v1/*`） | JWT Bearer Token | `Authorization: Bearer <access_token>` |
| WebSocket 行情（`/ws/v1/*`） | 连接时 QueryString | `?token=<access_token>` |
| SSE Agent 流（`/sse/v1/*`） | 连接时 QueryString | `?token=<access_token>` |
| 内部 Tool API（`/internal/v1/*`） | HMAC-SHA256 签名 | `X-Internal-Sig: <hmac_hex>` + `X-Internal-Ts: <utc_ms>` |

### 1.4 错误码表

| code | HTTP Status | 含义 | 处理建议 |
|:---|:---|:---|:---|
| `0` | `200` | 成功 | — |
| `1001` | `401` | Token 缺失 | 重新登录 |
| `1002` | `401` | Token 过期 | 用 Refresh Token 换新 Access Token |
| `1003` | `401` | Token 无效/篡改 | 清除本地凭证，重新登录 |
| `1004` | `403` | 权限不足 | 检查账户角色 |
| `1005` | `403` | HMAC 签名校验失败 | 检查内部密钥配置 |
| `2001` | `400` | 请求参数校验失败 | 查看 `msg` 字段获取具体字段错误 |
| `2002` | `404` | 资源不存在 | 检查资源 ID |
| `3001` | `503` | Futu OpenD 连接断开 | 等待自动重连或检查 OpenD 进程 |
| `3002` | `503` | Redis 不可用 | 检查 Redis 服务状态 |
| `3003` | `503` | 外部 API 熔断中 | 等待熔断器半开（约 60s） |
| `5000` | `500` | 内部未知错误 | 查看后端日志（携带 `trace_id`） |

### 1.5 分页约定

支持分页的接口统一使用 cursor-based 分页：

```json
// 请求
GET /api/v1/orders?cursor=<last_id>&limit=50

// 响应 data 结构
{
  "items": [...],
  "next_cursor": "ord_abc123",
  "has_more": true
}
```

---

## 二、认证接口（`/api/v1/auth`）

### 2.1 登录

```
POST /api/v1/auth/login
```

**Request Body:**
```json
{
  "password": "string"
}
```

> 个人系统无多用户，只验证主密码。

**Response `data`:**
```json
{
  "access_token": "eyJ...",
  "expires_in": 900,
  "token_type": "Bearer"
}
```

> Refresh Token 通过 `Set-Cookie: refresh_token=...; HttpOnly; Secure; SameSite=Strict` 写入浏览器，前端无需手动处理。

### 2.2 刷新 Token

```
POST /api/v1/auth/refresh
```

> 不需要 Request Body，自动读取 HttpOnly Cookie 中的 Refresh Token。

**Response `data`:** 同 2.1

### 2.3 登出

```
POST /api/v1/auth/logout
```

> 清除服务端 Refresh Token 黑名单记录，并通知客户端清除 Cookie。

---

## 三、行情接口（`/api/v1/market`）

### 3.1 获取实时快照

```
GET /api/v1/market/quote?symbols=AAPL,TSLA,00700.HK
```

**Response `data`:**
```json
[
  {
    "symbol": "AAPL",
    "name": "Apple Inc.",
    "price": 213.45,
    "change": 1.23,
    "change_pct": 0.58,
    "volume": 58234901,
    "turnover": 12430000000,
    "high": 215.10,
    "low": 211.80,
    "open": 212.00,
    "prev_close": 212.22,
    "market_cap": 3280000000000,
    "ts": 1719475200000
  }
]
```

### 3.2 获取历史 K线

```
GET /api/v1/market/kline?symbol=AAPL&period=D1&start=2024-01-01&end=2024-12-31&limit=500
```

| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `symbol` | `str` | ✅ | 标的代码（Futu 格式：`US.AAPL` 或 简写 `AAPL`） |
| `period` | `str` | ✅ | K线周期：`1m` `5m` `15m` `60m` `D1` `W1` `M1` |
| `start` | `str` | — | ISO 日期 `YYYY-MM-DD`，不传则取最近 `limit` 根 |
| `end` | `str` | — | ISO 日期，默认今日 |
| `limit` | `int` | — | 最多返回条数，默认 200，上限 2000 |

**Response `data`:**
```json
{
  "symbol": "AAPL",
  "period": "D1",
  "klines": [
    {
      "ts": 1719388800000,
      "open": 212.00,
      "high": 215.10,
      "low": 211.80,
      "close": 213.45,
      "volume": 58234901,
      "turnover": 12430000000
    }
  ],
  "source": "redis_cache"
}
```

### 3.3 主力资金流向

```
GET /api/v1/market/fund-flow?symbol=AAPL
```

**Response `data`:**
```json
{
  "symbol": "AAPL",
  "main_net_inflow": 1234567890,
  "retail_net_inflow": -234567890,
  "super_large_inflow": 987654321,
  "super_large_outflow": 123456789,
  "broker_queue": [
    { "broker": "Goldman Sachs", "buy_vol": 1000000, "sell_vol": 800000 }
  ],
  "ts": 1719475200000
}
```

### 3.4 期权链

```
GET /api/v1/market/option-chain?symbol=AAPL&expiry=2024-09-20
```

**Response `data`:**
```json
{
  "symbol": "AAPL",
  "expiry": "2024-09-20",
  "calls": [
    {
      "contract": "AAPL240920C00210000",
      "strike": 210,
      "last": 5.40,
      "bid": 5.35,
      "ask": 5.45,
      "volume": 12345,
      "open_interest": 56789,
      "iv": 0.28,
      "delta": 0.52,
      "gamma": 0.08,
      "theta": -0.15,
      "vega": 0.22
    }
  ],
  "puts": []
}
```

---

## 四、选股接口（`/api/v1/screener`）

### 4.1 执行选股

```
POST /api/v1/screener/run
```

**Request Body:**
```json
{
  "query": "市盈率低于20且ROE高于15%的美股科技股",
  "market": "US",
  "limit": 50
}
```

> `query` 字段直接传自然语言，后端 AI 引擎负责解析为过滤条件。

**Response `data`:**
```json
{
  "total": 128,
  "returned": 50,
  "stocks": [
    {
      "symbol": "GOOGL",
      "name": "Alphabet Inc.",
      "price": 180.50,
      "pe": 18.5,
      "roe": 0.23,
      "market_cap": 2200000000000,
      "sector": "Technology",
      "signals": ["MA_GOLDEN_CROSS", "VOLUME_SURGE"]
    }
  ],
  "filter_applied": {
    "pe": {"max": 20},
    "roe": {"min": 0.15},
    "market": "US",
    "sector": "Technology"
  }
}
```

---

## 五、订单管理接口（`/api/v1/oms`）

### 5.1 获取持仓

```
GET /api/v1/oms/positions
```

**Response `data`:**
```json
{
  "account_id": "acc_001",
  "total_asset": 1000000.00,
  "cash": 200000.00,
  "market_value": 800000.00,
  "unrealized_pnl": 45678.90,
  "unrealized_pnl_pct": 0.0456,
  "positions": [
    {
      "symbol": "AAPL",
      "name": "Apple Inc.",
      "qty": 1000,
      "avg_cost": 170.00,
      "current_price": 213.45,
      "market_value": 213450.00,
      "unrealized_pnl": 43450.00,
      "unrealized_pnl_pct": 0.2556,
      "today_pnl": 1230.00,
      "cost_ratio": 0.2135
    }
  ]
}
```

### 5.2 获取订单列表

```
GET /api/v1/oms/orders?status=all&cursor=<last_id>&limit=50
```

| `status` 值 | 含义 |
|:---|:---|
| `all` | 全部 |
| `pending` | 待成交 |
| `filled` | 已成交 |
| `cancelled` | 已撤销 |
| `partial` | 部分成交 |

### 5.3 模拟下单（沙箱）

```
POST /api/v1/oms/orders/simulate
```

**Request Body:**
```json
{
  "symbol": "AAPL",
  "side": "buy",
  "order_type": "limit",
  "qty": 100,
  "price": 210.00,
  "tif": "day",
  "note": "突破 MA20 入场"
}
```

> ⚠️ 当前仅支持模拟推演。实盘执行需 `REAL_TRADE_EXECUTE=true` 且接口切换至 `/api/v1/oms/orders/execute`。

---

## 六、Hermes Agent 接口（SSE）

### 6.1 发送问题并获取流式响应

```
POST /api/v1/agent/chat
Content-Type: application/json
Accept: text/event-stream
```

**Request Body:**
```json
{
  "message": "分析一下苹果公司最近的走势，给出买卖建议",
  "session_id": "sess_abc123",
  "context": {
    "current_symbol": "AAPL"
  }
}
```

**SSE Stream 格式：**
```
event: thinking
data: {"step": "tool_call", "tool": "get_broker_market_data", "args": {"action": "QUOTE", "symbol": "AAPL"}}

event: tool_result
data: {"tool": "get_broker_market_data", "result_summary": "AAPL 当前价 213.45，涨 0.58%"}

event: message
data: {"content": "## AAPL 走势分析\n\n根据最新数据...", "done": false}

event: message
data: {"content": "", "done": true, "session_id": "sess_abc123"}

event: error
data: {"code": 3001, "msg": "Futu OpenD 连接断开"}
```

| SSE Event 类型 | 含义 |
|:---|:---|
| `thinking` | Agent 推理步骤（Tool 调用计划） |
| `tool_result` | Tool 执行结果摘要 |
| `message` | 最终 Markdown 内容片段，`done=true` 标志结束 |
| `error` | 流中断错误 |

---

## 七、系统接口

### 7.1 健康检查

```
GET /api/v1/health
```

**Response `data`:**
```json
{
  "status": "healthy",
  "services": {
    "redis": {"status": "ok", "latency_ms": 1},
    "postgres": {"status": "ok", "latency_ms": 5},
    "futu_opend": {"status": "ok", "connected": true, "reconnect_count": 0},
    "ollama": {"status": "ok", "model": "llama3.1:8b"}
  },
  "uptime_seconds": 86400
}
```

### 7.2 前端日志上报

```
POST /api/v1/logs/frontend
```

**Request Body:**
```json
{
  "level": "error",
  "message": "WebSocket 重连超时",
  "context": {
    "component": "useWebSocket",
    "retry_count": 3
  },
  "ts": 1719475200000,
  "session_id": "sess_abc123"
}
```

### 7.3 客户端 APM 心跳

```
POST /api/v1/client/heartbeat
```

**Request Body:**
```json
{
  "platform": "ios",
  "app_version": "1.0.0",
  "device_id": "device_hash_abc",
  "fps": 58.5,
  "memory_mb": 145.2,
  "ws_latency_ms": 32,
  "ts": 1719475200000
}
```

---

## 八、WebSocket 协议（`/ws/v1/quotes`）

### 8.1 连接

```
ws://localhost:8000/ws/v1/quotes?token=<access_token>
```

### 8.2 客户端 → 服务端消息

```json
// 订阅行情
{"action": "subscribe", "symbols": ["AAPL", "00700.HK"], "channel": "quote"}

// 订阅 K 线实时更新
{"action": "subscribe", "symbols": ["AAPL"], "channel": "kline", "period": "1m"}

// 取消订阅
{"action": "unsubscribe", "symbols": ["AAPL"], "channel": "quote"}

// Ping（每 30s 发一次，防止超时断连）
{"action": "ping"}
```

### 8.3 服务端 → 客户端消息

```json
// 连接确认
{"type": "connected", "session_id": "ws_abc123", "ts": 1719475200000}

// Tick 推送（高频，约 500ms/次）
{
  "type": "tick",
  "symbol": "AAPL",
  "price": 213.45,
  "change_pct": 0.58,
  "volume": 58234901,
  "ts": 1719475200123
}

// K 线实时更新（最后一根 K 线更新）
{
  "type": "kline_update",
  "symbol": "AAPL",
  "period": "1m",
  "kline": {"ts": 1719475140000, "open": 213.10, "high": 213.60, "low": 213.00, "close": 213.45, "volume": 123456}
}

// Pong
{"type": "pong", "ts": 1719475200000}

// 服务端主动错误通知
{"type": "error", "code": 3001, "msg": "Futu OpenD 断开，正在重连..."}
```

---

## 九、内部 Tool API（`/internal/v1/*`）

> 仅供 Hermes Agent（同机或内网）调用，**禁止**通过公网访问。

### HMAC 签名生成规则

```python
import hmac, hashlib, time

def sign(secret: str, method: str, path: str, body: str = "") -> tuple[str, int]:
    ts = int(time.time() * 1000)
    payload = f"{method}\n{path}\n{ts}\n{body}"
    sig = hmac.new(secret.encode(), payload.encode(), hashlib.sha256).hexdigest()
    return sig, ts
```

请求时携带：
```
X-Internal-Sig: <sig>
X-Internal-Ts: <ts_ms>
```

服务端校验：时间戳偏差 > 30s 的请求直接拒绝（防重放）。

### 内部接口列表

| 端点 | 方法 | 对应 Tool | 说明 |
|:---|:---|:---|:---|
| `/internal/v1/market/quote` | `GET` | `get_broker_market_data(QUOTE)` | 行情快照 |
| `/internal/v1/market/history` | `GET` | `get_broker_market_data(HISTORY)` | 历史K线 |
| `/internal/v1/market/fund-flow` | `GET` | `get_broker_market_data(FUND_FLOW)` | 资金流向 |
| `/internal/v1/market/option-chain` | `GET` | `get_broker_market_data(OPTION_CHAIN)` | 期权链 |
| `/internal/v1/fundamental` | `GET` | `get_fundamental_data` | 基本面数据 |
| `/internal/v1/news/macro` | `GET` | `get_macro_news` | 宏观新闻 |
| `/internal/v1/news/company` | `GET` | `get_company_news` | 个股新闻 |
| `/internal/v1/screener/run` | `POST` | `screen_stocks` | 选股执行 |
| `/internal/v1/knowledge/search` | `POST` | `search_global_knowledge` | RAG检索 |
