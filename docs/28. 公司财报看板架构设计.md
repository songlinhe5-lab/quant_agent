# 公司财报看板架构设计（Filings & Financials Hub）

> **文档定位**：§二十九 公司财报看板的实现架构（HOW），产品定义见 `docs/01 §二十九`
> **版本**：V1.3（2026-08-31）· 任务序列 `FIN-01~08`（已录入 `docs/TODO-engine.md`；`FIN-01` 一手采集、`FIN-02` 归一化引擎、`FIN-03` 双时间轴存储、`FIN-04` Facade 收口 + 全端点已完成）
> **对标**：SEC EDGAR XBRL（一手事实层，官方免费）· OpenBB SEC Provider（申报标签→标准科目的**声明式** schema）· Daloopa（每个数字带原文页码，可审计）· Koyfin / TIKR（多期报表 + 同业对比工作流）
> **关联文档**：`docs/23` 业务 Facade · `docs/14` 数据源子服务 · `docs/19` 快照版本化 · `docs/11` 数据模型 · `docs/01 §二十` 数据中心（财报 RAG / 财报日历已落地）

---

## 一、目标与对标

### 1.1 要解决的问题

现状：仓库已有**零散的**基本面能力，但没有「公司财报」这个第一公民。

| 缺口 | 现状证据 | 业界方案 | 本模块交付 |
|:---|:---|:---|:---|
| 无一手申报源 | 全部数据来自二手聚合商（Futu / FMP / YF / Tushare） | SEC EDGAR 官方 XBRL | 三地一手源：EDGAR / 披露易 / 巨潮 |
| `get_fundamental` 是假基本面 | Futu `FUNDAMENTAL` 只返回 PE/PB/EPS 快照 5 字段 | 完整三表 + 附注 | 归一化事实层，科目级可查 |
| 无历史深度 | 各源接口只给最近若干期，且口径互斥 | 10~20 年标准化历史 | 事实表按期存全量，一次回填 |
| 无重述追踪 | 拿到什么算什么，改了不知道 | as-reported vs restated 双轨 | 双时间轴，重述自动 diff 告警 |
| 无同业/行业对比 | 只有单票视图 | 同业中位数 + 截面分位 | peer set + EDGAR `frames` 截面 |
| 财报只能问不能算 | `earnings-qa-panel` 仅 RAG 问答 | 结构化指标 + 文本双通道 | 数字走事实层，文本走 RAG |

### 1.2 「一手」的定义与三地口径

一手 = **发行人向法定披露平台提交的原始申报**，而非数据商加工结果。三地成熟度差异极大，必须分层对待，不能假装统一：

| 市场 | 一手源 | 结构化程度 | 数字层落法 | 文本层落法 |
|:---|:---|:---|:---|:---|
| 美股 | SEC EDGAR（`data.sec.gov`，免费无 Key） | **XBRL 全量事实**，逐 tag 带 `filed`/`accn`/`start`/`end` | 直接进事实表（权威） | 10-K/10-Q 原文进 RAG |
| 港股 | HKEXnews 披露易 | **仅 PDF**，无 iXBRL 公开接口 | 数字层退回 Futu `FINANCIALS`（已有） | PDF 进 RAG + LLM 抽取（须带页码） |
| A股 | 巨潮资讯网 CNINFO | **仅 PDF**（XBRL 未公开开放） | 数字层退回 Tushare `FINANCIALS`（已有） | 同上 |

> **诚实性红线**：港股/A股的数字层是**二手**，前端必须经 `DataSourceBadge` 标注来源与口径，禁止与 EDGAR 一手数字混在同一列不加区分（AGENTS §3）。

### 1.3 非目标（YAGNI）

- ❌ 不自建 PDF→三表 的通用解析器（港股/A股结构化交给已有 Futu/Tushare，PDF 只做文本与定点抽取）
- ❌ 不做盈利预测模型与 DCF 估值（估值属 §二十三 优化器与策略侧）
- ❌ 不做 GICS 行业分类（商业授权），改用 SEC SIC / 申万 / Futu 板块，并在 UI 标明分类体系
- ❌ 不做实时财报推送（财报日历已在 §二十，本模块只消费）

---

## 二、数据流与后端模块

```
一手采集（仅 data_subservice，AGENTS §2）        归一化              双时间轴存储           消费
┌────────────────────────────┐
│ filings_worker              │
│  ├ SEC  submissions/        │   ┌──────────────┐   ┌────────────────┐
│  │      companyfacts/frames │──→│ ConceptMapper │──→│ financial_facts│──┐
│  ├ HKEX titleSearchServlet  │   │ (声明式映射)   │   │ (PG · 双时间轴) │  │
│  └ CN   hisAnnouncement     │   └──────┬───────┘   └────────────────┘  │
└────────────┬───────────────┘          │            ┌────────────────┐  │
             │ HTTP+HMAC                ↓            │ filings (PG)   │  │
      DataSourceRouter            AccountingCheck    │ 申报索引/原文址 │  │
             │                    (勾稽校验)          └────────────────┘  │
   既有源（二手数字层）                                ┌────────────────┐  │
   Futu FINANCIALS / VALUATION ──────────────────────→│ Parquet 宽表   │  │
   Tushare FINANCIALS ────────────────────────────────│ (docs/19 快照) │  │
                                                      └────────────────┘  │
                                                                          ↓
                             FinancialsFacade ──→ routers/financials.py ──→ features/financials/
                                     │                                         · 多期报表 / 趋势
                                     ├──→ AnalyticsEngine（DuPont / 质量分 / 现金流勾稽）
                                     ├──→ PeerEngine（同业中位数 + frames 截面分位）
                                     └──→ 既有 RAG（pgvector, category=financial_report）
```

**模块落位**（`routers/` 只校验转发，业务在 `services/`，纯数值在 `domain/`）：

| 模块 | 路径 | 职责 |
|:---|:---|:---|
| 一手采集 | `data_subservice/filings_worker.py`（新建） | 三源申报索引与原文获取；SEC 限速与 UA 合规；仅本层触外网 |
| SEC 内部实现 | `data_subservice/_internal/sec_edgar.py`（新建） | `submissions` / `companyfacts` / `frames` 三端点封装 + 分页与缓存 |
| 概念映射 | `backend/domain/financials/concept_map.json` + `mapper.py`（新建） | XBRL tag / Futu field_id / Tushare 列 → 标准科目；**数据即配置**，禁硬编码 if-else |
| 期间与勾稽 | `backend/domain/financials/periods.py` · `checks.py`（新建） | YTD 拆分、Q4 推导、三表勾稽断言 |
| 分析引擎 | `backend/domain/financials/analytics.py`（新建） | common-size / TTM / DuPont / 质量三分（纯函数，无 IO） |
| 同业引擎 | `backend/services/financials/peers.py`（新建） | peer set 解析 + 行业分位聚合 |
| 服务编排 | `backend/services/financials/service.py`（新建） | 采集→归一→落库→读取；重回填走进程池（禁路由阻塞） |
| 视图装配 | `backend/services/financials/views.py`（新建） | 长表事实 → 多期宽表 / 事实明细 / 重述 diff（纯函数，无 IO） |
| 回填任务登记簿 | `backend/services/financials/jobs.py`（新建） | 进程内任务状态 + 进程池流水线（payload 归一在此卸载） |
| filings 薄适配器 | `backend/services/datasource/adapters/filings.py`（新建） | dict 响应 → `Result`；经 `data_source_router.fetch_filings()`，不直连外网 |
| 响应装配 | `backend/routers/financials_schemas.py`（新建） | 统一信封 + 错误码 → HTTP 状态翻译，保持路由薄 |
| Facade 收口 | `backend/services/datasource/business/fundamental.py`（扩展） | 新增 `get_statements` / `get_facts` / `get_filings` / `get_restatements` / `backfill`，与既有 `get_fundamental_merged` 并存 |
| 路由 | `backend/routers/financials.py`（新建） | 见 §六 |

**采集合规**（写 `filings_worker` 必看）：

- SEC 要求描述性 `User-Agent`（含联系邮箱），缺失即 403；请求频率 ≤ 10 req/s，超限会被封 IP → 归入 `data_subservice` 既有限流器，`rate_limit` 不计入熔断失败计数（AGENTS §4）。
- `companyfacts` 单公司响应可达数 MB（Apple 约 3.8MB / 500+ 概念），必须落盘缓存 + 按 `filed` 增量，禁止每次全量拉。
- 披露易 `titleSearchServlet.do` 与巨潮 `hisAnnouncement/query` 都是**页面后端接口而非开放 API**，必须 fixture 测试锁响应结构，页面改版时显式失败而不是静默拉空。
- **披露易实测契约（2026-08-31，阅文 00772 中报公告验证）**：① 必须两步——先 `prefix.do`（JSONP）解析内部 `stockId`，仅传 `stockCode` 恒查不到；② 日期参数是 `fromDate`/`toDate` 且格式 `YYYYMMDD`，传 `pubFrom`/`pubTo` 或带横杠格式会被**静默忽略**（返回全量旧数据，最危险的坑）；③ `result` 是字符串化 JSON 数组需二次 `loads`，无记录时为 `"null"` 或 `"[]"` 字符串；④ 字段全大写（`TITLE`/`DATE_TIME` 为 `DD/MM/YYYY HH:MM`/`FILE_TYPE`/`FILE_LINK`）。fixture 已按真实响应重写。
- 新增能力声明 `DS_CAPABILITIES=...,filings`；SEC 采集节点固定在美国节点，禁止从 CN-DATA 节点直连（`docs/06` 拓扑）。

**FIN-04 落地踩坑（2026-08-31，改读/写链路前必看）**：

- **源名两套，别混用**：`DataSourceRegistry` 按适配器 `name` 建桶，所以取数只能用 `"filings"`；写进事实表的溯源标签是 `"sec"`。用 `"sec"` 去 fetch 只会得到 `SOURCE_NOT_FOUND`。代码里固定为 `FILINGS_REGISTRY_SOURCE`。
- **后台任务自持会话**：`schedule_backfill(session_factory, ...)` 收的是**会话工厂**，不是会话——请求一返回，路由那个 `AsyncSession` 就被关闭，复用会把正在跑的回填拦腰折断。
- **进程池卸载对象必须是模块级纯函数**（`jobs.transform_payload`）：闭包 / 绑定方法不可 pickle，`run_cpu_bound` 会静默回退线程池，GIL 下等于没卸载。
- **采集异常也得把任务推到终态**：`_fetch_payload` 必须在 `try` 内，否则前端只能对着 `pending` 转圈。
- **勾稽只标不丢，且必须按报表归属**：`CHECK_STATEMENTS` 把 `balance_identity`→balance、`cash_flow_reconciliation`→cash、`gross_profit`→income 绑定，否则一条时点失败会涂到不相干报表；推导值（Q4 = FY − 9M）不参与勾稽——拿算出来的数验等于自己验自己。
- **多版本仲裁键是 `(concept, 期间列)`**：只按列比会把同列的其他科目当旧版本静默丢掉。
- **状态码以异常为准**：`fin_source_degraded` 一个码承载三种成因（限流 429 / 源不可用 502 / 源未接入 501），客户端退避策略全靠这个码，路由不得回落到默认映射把它抹平。

---

## 三、事实层：归一化与双时间轴

这是本模块唯一不可外包的核心。做对了后面全是查询，做错了全是脏数。

### 3.1 声明式概念映射

标准科目 → 候选标签**有序链**，先命中先用，全部落空则该科目为 `null`（不猜、不插值）：

```json
{
  "revenue": {
    "us-gaap": ["RevenueFromContractWithCustomerExcludingAssessedTax",
                 "Revenues", "SalesRevenueNet"],
    "ifrs-full": ["Revenue"],
    "futu": ["field_id:3001"],
    "tushare": ["total_revenue"],
    "statement": "income", "sign": 1
  }
}
```

映射表**必须带版本号**并入 git；新增标签链走 PR + 单测，禁止在服务代码里补 if-else。测试方式：抽 20 家覆盖不同行业的公司做 golden case，映射改动导致任一 golden 变化即失败。

### 3.2 双时间轴（as-reported vs restated）

每条事实同时携带两条时间轴，这是防前视偏差的地基，也是重述追踪的副产品：

| 轴 | 字段 | 含义 |
|:---|:---|:---|
| 业务时间（valid time） | `period_start` / `period_end` | 会计期间本身 |
| 系统时间（knowability） | `filed_at` / `accession_no` | 该数字**何时可被市场知晓** |

去重与取值规则（EDGAR 实践，踩过的坑写死在这）：

1. 唯一键 = `(entity_id, concept, period_start, period_end, unit)`。**禁止只按 `fy` 去重**——比较期数字与本期共用同一 `fy` 标签，按 `fy` 去重会静默丢数。
2. `value_as_reported` = 该唯一键下 `filed_at` **最早**的值（首次披露，冻结不改）。
3. `value_latest` = `filed_at` **最新**的值（含修正与重述）。
4. 两者不等 → 置 `restated=true`，进重述 diff 面板。
5. PIT 查询一律 `filed_at <= as_of`，回测/因子只允许读这条通路（对齐 `financial_pit.PointInTimeStore` 现有 `announce_date` 语义，本任务把它从内存单例落到 PG）。

> **用途分工**：回测、因子、信号 → `as_reported`；「这家公司十年真实毛利率走势」→ `latest`。UI 必须让用户看得见当前在看哪一条。

### 3.3 期间推导（10-Q 的 YTD 陷阱）

10-Q 的流量科目可能是**单季**也可能是**累计**，只能靠 `(start, end)` 跨度判定，不能信 `fp` 标签：

- 跨度 ≈ 91 天 → 单季，直接用。
- 跨度 ≈ 182 / 273 天 → 累计，Q2 = H1 − Q1，Q3 = 9M − H1。
- **Q4 永远是推导值**：Q4 = FY − 9M（EDGAR 无独立 Q4 申报），推导结果必须标 `derived=true`，前端显示浅色角标。
- 存量科目（`Assets` / `StockholdersEquity`）是时点值，EDGAR `frames` 须用 `CY2024Q3I` 的 `I` 后缀，用错后缀返回 404。

### 3.4 勾稽校验（落库前门禁）

| 校验 | 断言 | 失败处理 |
|:---|:---|:---|
| 资产负债恒等 | `assets ≈ liabilities + equity`（相对误差 < 0.5%） | 标 `check_failed`，仍入库但前端标红，**禁止静默丢弃** |
| 现金流勾稽 | `cfo + cfi + cff ≈ Δcash` | 同上 |
| 毛利一致 | `revenue − cogs ≈ gross_profit`（若三者齐备） | 同上 |
| 分部加总 | 分部收入合计 ≈ 合并收入 | 仅告警（分部口径常不完备） |

---

## 四、Pydantic Schema 与存储

```python
class FinancialFact(BaseModel):
    """事实表最小单元。一行 = 一个科目在一个期间的一个版本。"""
    entity_id: str                    # 内部主键：US:CIK0000320193 / HK:00700 / CN:600519
    concept: str                      # 标准科目名（concept_map 的 key）
    statement: Literal["income", "balance", "cash"]
    period_start: date | None         # 时点科目为 None
    period_end: date
    fiscal_year: int
    fiscal_period: Literal["Q1", "Q2", "Q3", "Q4", "FY"]
    unit: str                         # USD / HKD / CNY / shares
    value_as_reported: float
    value_latest: float
    restated: bool = False
    derived: bool = False             # Q4 或 累计拆分的推导值
    filed_at: date
    accession_no: str | None          # EDGAR accn；港A为公告 id
    source: str                       # sec / futu / tushare
    source_tag: str                   # 原始标签，保留以便回溯映射错误
    check_failed: list[str] = []      # 勾稽失败项

class FilingRecord(BaseModel):
    entity_id: str
    form_type: str                    # 10-K / 10-Q / 8-K / 年報 / 年度报告
    fiscal_year: int
    filed_at: date
    accession_no: str
    doc_url: str
    lang: Literal["en", "zh"]
    rag_indexed: bool = False

class StatementView(BaseModel):
    """GET /financials/statements 响应：多期宽表。"""
    entity_id: str
    statement: str
    periods: list[str]                        # ["FY2021", ..., "FY2025"]
    rows: list[dict[str, Any]]                # {concept, label, values[], common_size[], yoy[]}
    basis: Literal["as_reported", "latest"]
    currency: str
    source_mix: dict[str, int]                # {"sec": 42, "futu": 0} —— 前端据此标来源
    integrity: dict[str, Any]                 # 勾稽结果摘要，不合格必须透出
```

**存储分工**：

- `financial_facts` / `filings` → **PG**（Alembic 迁移，唯一键见 §3.2；`(entity_id, concept, period_end)` 联合索引）。
- 多期宽表与截面面板 → **Parquet**（`data/snapshots/financials/`，复用 `docs/19` 目录约定），供因子与回测按 `data_snapshot_id` 读取。
- 财报原文 → 既有 `WebpageKnowledgeBase`（pgvector，`category=financial_report`），不新建向量库。

---

## 五、分析引擎

### 5.1 单公司

| 分析 | 输出 | 说明 |
|:---|:---|:---|
| 结构化报表 | common-size（收入/总资产为基）+ YoY/QoQ + 5/10 年 CAGR | 基础层 |
| TTM 滚动 | 四季滚动收入/利润/现金流 | 财年错位公司必须用 TTM 比 |
| DuPont 分解 | ROE = 净利率 × 周转率 × 权益乘数（可下钻五因子） | 定位 ROE 变化来源 |
| 现金流质量 | CFO/净利润、应计比率、FCF 转化率、资本开支强度 | 利润含金量 |
| 质量三分 | Piotroski F（9 项）· Altman Z · Beneish M | 均为公开可复算模型，须展示分项而非只给总分 |
| 重述追踪 | as-reported vs latest 的科目级 diff | 双时间轴的直接产物，业界少见 |

> 三个分数必须展示**分项明细与阈值**。只给一个总分而不给构成，等于让用户信一个黑箱——违反 AGENTS §5 数字可溯源。

### 5.2 同业与行业

- **peer set 解析**：SEC SIC（美股，来自 `submissions`）/ 申万行业（A股，Tushare）/ Futu 板块（港股）；三者体系不同，UI 必须标注用的是哪套。支持用户手工固定 peer 清单并保存（分析师的同业判断常优于机械分类）。
- **截面分位**：美股用 EDGAR `frames` 端点——一次请求拿到**全市场**某个 tag 在某期的取值，天然适合算行业分位，避免 N 次单票请求。
- **行业聚合**：中位数 / 四分位 / 收入加权综合值；样本 < 8 家时标注「样本不足」并禁止出分位结论（对齐 §9.7 防噪音）。

### 5.3 文本层（LLM，强制溯源）

- **MD&A / 风险因素 YoY diff**：对同一公司相邻两年年报做章节级文本相似度与差异高亮。学界依据：Cohen–Malloy–Nguyen《Lazy Prices》（JF 2020）——年报措辞的显著变化对未来收益有预测力。这是纯文本能力，投入低、区分度高。
- **定点抽取**：仅对港股/A股 PDF 做「指定科目 + 分部/指引」抽取，每个抽取值必须带 `source_page` + `source_text`；缺任一即判为抽取失败并丢弃，**不允许 LLM 给出无出处的数字**（AGENTS §5）。
- 复用既有 `routers/rag.py` 与 `earnings-qa-panel`，本模块只补「引用可跳回原文页」的定位信息。

> **FIN-08 交付注记（2026-08-31）**：文本层已交付——`domain/financials/textlayer.py`（零 IO 纯函数）+ 子服务 `DOC_TEXT` 通道（EDGAR 原文拉取 + 确定性 HTML 清洗，截断标 `truncated`）+ `GET /text/diff/{entity}`（缺省自动取最近两份 10-K，章节锚点缺失不产出；重写阈值 0.80 词级相似度可复算，重写章节排前）+ `POST /text/extractions`（缺 `source_page`/`source_text`/`value` 任一即拒并报原因）。`rag_citation` 结构（url/quote/source_page）已就位，`filing-timeline` 的「送 RAG」按钮待任务端点后接入。港A PDF 抽取管线（LLM 侧）未接——校验器已就位，PDF 解析源接入后直接复用。
>
> **FIN-08b/08c 交付注记（2026-09-01）**：文本层消费闭环完成——`services/financials/rag_bridge.py`（章节锚点优先 + 滑动窗口兜底切分，幂等 id `filing_{md5[:12]}_{i}`，embed/save 可注入；EDGAR 无页码概念**不伪造 source_page**）+ `POST /financials/filings/{entity}/{accession}/ingest`（经 Registry `DOC_TEXT` 拉文本，向量化/写库 `to_thread` 卸载；成功回写 `FilingRecord.rag_indexed`；失败如实报 `fin_source_degraded`/`fin_not_found`，不静默丢数）+ 前端 `mda-diff-panel`（重写章节排前标 amber，片段删红/增绿）与 `filing-timeline`「送 RAG」按钮（成功回显 +N 片段，按钮消失）。chunk 经 `WebpageKnowledgeBase`（category=`financial_report`）进入既有 `rag/chat` / `rag/search` 检索链路。

---

## 六、API 端点（`routers/financials.py`，统一 `{status, message, data, timestamp}`）

| 方法 | 路径 | 说明 | 状态 |
|:---|:---|:---|:---|
| `GET` | `/financials/statements/{entity}` | 多期报表（`statement` / `basis` / `as_of` / `limit`），entity 兼容 `US:CIK…` 与 `US-CIK…` | ✅ FIN-04 |
| `GET` | `/financials/facts/{entity}` | 科目级明细（含双时间轴，传 `as_of` 即 PIT 查询） | ✅ FIN-04 |
| `GET` | `/financials/filings/{entity}` | 申报归档时间轴（原文链接 + RAG 索引状态） | ✅ FIN-04 |
| `GET` | `/financials/restatements/{entity}` | 重述 diff 清单（首次披露 vs 最新） | ✅ FIN-04 |
| `POST` | `/financials/backfill` | 历史回填：登记任务→挂后台→立即返 `job_id`（禁止在请求里等采集） | ✅ FIN-04 |
| `GET` | `/financials/jobs/{job_id}` | 回填任务状态（pending / running / success / failed） | ✅ FIN-04 |
| `GET` | `/financials/analytics/{entity}` | DuPont + 现金流质量 + 质量三分 + TTM（`as_of` PIT / `market_cap` 由行情侧传入） | ✅ FIN-05 |
| `GET` | `/financials/peers/{entity}` | 同业中位数与截面分位（`peer_set` 可覆盖；样本 <8 → 422 禁出分位） | ✅ FIN-06 |
| `GET` | `/financials/text/diff/{entity}` | MD&A / 风险因素 YoY diff（`accession_a/b` 指定年报对，缺省自动取最近两份 10-K；不足两份 → 404） | ✅ FIN-08 |
| `POST` | `/financials/text/extractions` | 定点抽取批量溯源校验（缺 `source_page`/`source_text`/`value` 任一即拒并报原因） | ✅ FIN-08 |
| `POST` | `/financials/filings/{entity}/{accession}/ingest` | 申报原文 → RAG 知识库（幂等切分 + 向量化 + 写库；成功回写 `rag_indexed`） | ✅ FIN-08b |
| `GET` | `/financials/coverage/{entity}` | 核心科目 × 最近 N 财年覆盖盘点（`years` 默认 10；缺失期显式列出，禁止补零） | ✅ FIN-09 |
| `POST` | `/financials/backfill-batch` | 目标池批量回填（单批 ≤50，逐实体挂后台任务立刻返 `job_id` 清单） | ✅ FIN-09 |

错误码（`error_code` → HTTP 状态，路由 `financials_schemas.ERROR_STATUS` 为 SSOT）：

| `error_code` | 默认状态 | 语义 |
|:---|:---|:---|
| `fin_entity_not_found` | 404 | 实体无法解析（含 ticker 不在 EDGAR 对照表），**不猜 CIK** |
| `fin_no_xbrl_coverage` | 404 | 未提交 XBRL 的公司（20-F / 40-F）与空事实表 |
| `fin_job_not_found` | 404 | 回填任务不存在（与实体不存在区分，否则前端排障只能猜） |
| `fin_not_found` | 404 | 申报对不存在（不足两份 10-K / 指定 accession 不存在或缺原文 URL）——校验失败不发数据源请求 |
| `fin_bad_request` | 400 | 入参不合法（`statement` 枚举 / `as_of` 非 `YYYY-MM-DD`） |
| `fin_source_degraded` | 502（异常自带为准） | 限流 429 / 源不可用 502 / 源未接入 501 |
| `fin_backfill_failed` | 502 | 归一 / 落库流水线抛异常（任务同时被推到 `failed`） |
| `fin_peer_sample_too_small` | 422 | 同业样本 < 8 家，禁止出分位结论（FIN-06） |
| `fin_not_implemented` | 501 | 预留占位（FIN-04 曾用于 `analytics` / `peers`，FIN-05/06 已全部转正，当前路由无 501） |

---

## 七、前端组件（`features/financials/`）

| 组件 | 引擎 | 说明 |
|:---|:---|:---|
| `statement-grid.tsx` | AG Grid | 多期宽表，行=科目、列=期间；支持 common-size 切换、口径切换、点击科目下钻到申报原文 |
| `trend-chart.tsx` | ECharts | 收入/利润/现金流多期趋势 + 利润率副轴 |
| `dupont-panel.tsx` | ECharts | ROE 三/五因子分解瀑布 |
| `peer-compare.tsx` + `peer-scatter.tsx` | ECharts scatter | 同业截面散点（`peer_rows` 明细行，本体高亮 + p25/p75 markLine）；无明细行退回区间条 |
| `quality-scorecard.tsx` | 纯 DOM | F/Z/M 三分及**分项明细**（阈值可见） |
| `filing-timeline.tsx` | 纯 DOM | 申报归档时间轴，跳原文 / 送 RAG |
| `restatement-diff.tsx` | AG Grid | 首次披露 vs 最新，差异标红 |

- K 线主图库（Lightweight-Charts）在本模块**不使用**；全部为低频分析图，走 ECharts（AGENTS §3）。
- 行数硬顶：feature 页 250 / 组件 150；空态三件套 `InitOverlay` / `EmptyState` / `DataSourceBadge` 必挂。
- 每张表/图必须显示：数据口径（as-reported / latest）、来源（sec / futu / tushare）、最近申报日期；勾稽失败与推导值分别标红、标浅色角标。

> **FIN-07 交付注记（2026-08-31）**：七组件 + 工作台已交付（`/financials` 路由 + 侧栏入口）。两处有意降级：① `peer-compare` 散点图降级为分位区间带——FIN-06 后端只回聚合（中位数/四分位/分位），不同业明细行，后端补截面明细后升级散点；② `filing-timeline` 暂只展示 RAG 索引状态与原文链接，"送 RAG"按钮等 FIN-08 提供任务端点后再挂，不放假按钮。

---

## 八、任务序列与依赖

| 任务 | 内容 | 依赖 | 预估 |
|:---|:---|:---|:---|
| **FIN-01** | `filings_worker` + SEC 三端点封装 + 披露易/巨潮索引；限流·UA·fixture 测试 | 无（可先行） | BE 3d |
| **FIN-02** | `concept_map.json` + 归一化引擎 + 期间推导 + 勾稽校验（golden case 20 家） | FIN-01 | BE 3d |
| **FIN-03** | 双时间轴存储：`financial_facts` / `filings` PG 迁移 + PIT 查询 + Parquet 宽表落盘 | FIN-02 | BE 2d |
| **FIN-04** | Facade 收口 + `routers/financials.py` 全端点 + 回填任务 | FIN-03 | BE 2d ✅ |
| **FIN-05** | 分析引擎：common-size / TTM / DuPont / 现金流质量 / F·Z·M（纯函数 + 手算对照） | FIN-03 | BE 2d |
| **FIN-06** | 同业引擎：peer set 解析 + `frames` 截面分位 + 行业聚合 | FIN-04 | BE 2d |
| **FIN-07** | 前端 `features/financials/` 七组件 + 个股工作台入口 | FIN-05/06 | FE 3d |
| **FIN-08** | 文本层：MD&A/风险因素 YoY diff + 定点抽取强制溯源 + RAG 引用定位 | FIN-01 + 既有 RAG | BE 2d / FE 1d |

```
FIN-01 ──→ FIN-02 ──→ FIN-03 ──┬──→ FIN-04 ──→ FIN-06 ──┐
                               └──→ FIN-05 ─────────────┴──→ FIN-07
FIN-01 ──────────────────────────────→ FIN-08（与主链并行）
```

**建议先做 FIN-01~03**：一手事实层是唯一不可替代的部分，分析与 UI 都可以在它之上迭代；反过来先做 UI 会被脏数据反复推翻。

---

## 九、验收标准

| 维度 | 指标 | 验收线 |
|:---|:---|:---|
| 正确性 | 20 家 golden 公司三表科目 vs 申报原文人工核对 | 逐科目一致，映射改动触发 golden 失败 |
| 正确性 | 勾稽校验 | 资产负债/现金流恒等误差 < 0.5%，超限标红不静默 |
| PIT | `as_of` 查询 | 任一历史日期查询结果不含该日之后 `filed_at` 的事实（Pytest 断言） |
| 覆盖 | 美股 XBRL 覆盖 | 目标池 10 年历史缺失期 < 5%，缺失显式列出而非补零 |
| 合规 | SEC 采集 | UA 合规 + ≤10 req/s；限流命中走退避，不计熔断 |
| 溯源 | LLM 抽取值 | 100% 带 `source_page` + `source_text`，缺失即丢弃 |
| 性能 | 单票 10 年三表 + 分析 | 缓存命中 < 1s；冷取 < 8s |
| 测试 | 后端覆盖率 | ≥ 80%（CI 门禁），单测禁打真实 SEC/Futu/外网 |

> **FIN-09 交付注记（2026-09-01）**：数据运维就位——`services/financials/coverage.py`（纯函数审计：核心五科目 × 最近 N 个 FY，缺失期显式列出、`coverage_pct` 可手算）+ `GET /coverage/{entity}` + `POST /backfill-batch`（单批 ≤50 防限流，逐实体复用既有 `schedule_backfill`）+ `snapshot_daemon`（`FINANCIALS_SNAPSHOT_HOUR` 默认 6 点，把全部已回填实体的宽表重写进当日 `snap_financials_YYYYMMDD`，docs/19 引用链保活；刷新失败只告警不退出，已挂 `worker.py` 主节点）。部署项：`.env.example` 已补 `SEC_EDGAR_USER_AGENT`（SEC 合规 UA，不配则子服务告警且可能被封 IP）。**待人工**：20 家 golden 三表科目人工核对；`concept_map.json` 里 `ifrs-full` / `tushare` / `futu` 三条映射链 `verified: false`，接真源实测后转正。
>
> **FIN-10 交付注记（2026-09-01）**：性能优化——① 子服务 `get_document_text` 落盘缓存（immutable 文档永不过期，`doc_text_{md5(url)}`，响应带 `cached` 标记，验收「缓存命中 < 1s」达成关键）；② `get_text_diff` 词级 SequenceMatcher 卸 `asyncio.to_thread`，事件循环不被秒级 diff 阻塞；③ `peer_view` 补 `peer_rows` 同业明细行，FIN-07 的「散点降级为区间带」解除（`peer-scatter.tsx` 散点 + 本体高亮，兼容降级路径）。
>
> **FIN-11 交付注记（2026-09-01）**：回填任务登记簿 PG 持久化——新表 `financial_jobs`（迁移 `fin10a`，部署时需 `alembic upgrade head`）。内存 dict 仍是任务状态 SSOT，PG 为 best-effort 快照：重启后 `GET /jobs/{id}` 内存 miss 落库、历史 pending/running 由启动钩子收敛为 `failed/interrupted by restart`（终态不误伤，表未迁移不拦启动）。快照写失败只告警，回填照常推进（本身幂等重放）。

---

## 十、红线速查

| 禁止 | 替代 |
|:---|:---|
| 主服务直连 `data.sec.gov` / 披露易 / 巨潮 | 一律经 `data_subservice` + `DataSourceRouter` |
| 按 `fy` 去重 XBRL 事实 | 按 `(concept, start, end, unit)` 唯一键 |
| 回测/因子读 `value_latest` | 读 `value_as_reported` + `filed_at <= as_of` |
| 缺失科目补 0 或线性插值 | 置 `null`，前端显式留白 |
| LLM 给出无页码出处的数字 | 抽取失败即丢弃并报告 |
| 港A二手数字与 EDGAR 一手混列不标注 | `DataSourceBadge` 标源与口径 |
| 服务代码里写标签映射 if-else | 改 `concept_map.json` + golden 测试 |
