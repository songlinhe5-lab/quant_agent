# ============================================================
# Quant Agent - Data Subservice 依赖收口 (纯数据源子服务)
# ============================================================
# ⚠️ 警告：本文件为「全量 SDK 清单」，仅用于本地开发/调试参考，
#    NOT 用于生产镜像构建！生产镜像由 data_subservice/Dockerfile 经
#    pyproject.toml 的细粒度 extra (datasource-us / datasource-cn / datasource-us-aux)
#    按节点能力隔离安装，避免单镜像打全量 SDK 造成体积膨胀。
#    若误用 `pip install -r requirements.txt` 构建，会把 futu+akshare+tushare+yfinance
#    全部装进镜像，体积从 ~400MB 飙到 500MB+。
# - 子服务独立运行在远程 VPS，主服务 (backend) 经 DataSourceRouter 走 HTTP 调用，
#   主服务 requirements 不再包含任何第三方数据源依赖，避免在主节点暴露直连 API。
# - 版本号取下限(>=)，不锁死，给解析器留余地；如需可复现构建请改用
#   requirements.lock / poetry.lock。
# - 本地安装：python3 -m pip install -r data_subservice/requirements.txt (仅调试用)
# ============================================================

# ── 第三方数据源 SDK (子服务直连核心) ──
futu-api>=10.7.0         # Futu OpenD 直连 (港股/美股/期权)
tushare>=1.4.0           # A股主源 (2000积分/200元档)
akshare>=1.14.0          # AkShare (A股兜底源, 新浪源)
yfinance>=0.2.40         # Yahoo Finance (美股/ETF)
protobuf>=4.0.0          # futu-api 内部行情 protobuf 编解码依赖

# ── 数据处理 ──
pandas>=2.0.0
numpy>=1.24.0

# ── 子服务框架 ──
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0
redis>=5.0.0
python-dotenv>=1.0.0
prometheus-client>=0.20.0   # 子服务 /metrics 指标暴露

# ── 通用 ──
requests>=2.31.0
rich>=13.0.0                  # _internal/logger 日志美化 (运行时必需)
tenacity>=8.2.0               # _internal/retry_utils 重试装饰器 (运行时必需)
